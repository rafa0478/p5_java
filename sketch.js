function setup() {
  createCanvas(500, 400);
  background("purple");
}

function draw() {
  
  fill("red");
  stroke("blue");
  if(mouseIsPressed){}
rect(mouseX,mouseY,50,10);//retangulo(pos x,pos y,tam x,tam y)
  
  
}
